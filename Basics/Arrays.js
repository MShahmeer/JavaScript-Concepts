let selectedColors = ["red", "blue"];

console.log(selectedColors);

//now the arrays can expend, and can have more than 1 type of variables in that

//Array is an object itself

console.log((selectedColors[2] = 1));
